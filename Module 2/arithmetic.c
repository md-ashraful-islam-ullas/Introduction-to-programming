#include <stdio.h>
int main()
{
    int a ;
    float b;
    int sum;
    a = 15, b = 2, sum = a+b;
    // printf("add = %d\n", sum);
    // int sub;
    // sub = a-b;
    // printf("sub = %d\n", sub);
    // int mult;
    // mult = a*b;
    // printf("multiplication = %d\n", mult);
    float div;
    div = a/b;
    printf("divition = %.2f\n", div);
    return 0;
}